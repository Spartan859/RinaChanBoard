<view class="{{className}}" style="{{style}}" onTap='tapHandler' class="custom_label_box">
    <view a:if='{{catchTap}}' class='custom_label_mark' catchTap='catchtapHandler'></view>
    <label for="{{for}}">
      <slot></slot>
    </label>
</view>